---
phase: 01-app-container
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-requirements.txt
  - .dockerignore
  - Dockerfile
  - entrypoint.sh
  - app.py
  - backup_tennis_db.sh
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - OPS-01
  - OPS-02
  - OPS-03

must_haves:
  truths:
    - "docker build completes without error and produces an image under 200MB"
    - "docker run starts the container and curl localhost:5000/health returns HTTP 200 with {status: ok}"
    - "Container process runs as non-root user appuser"
    - "Setting DB_PATH env var causes app to read/write SQLite at that path"
    - "Gunicorn starts with exactly 1 worker and eventlet worker class"
    - "Application logs appear in docker logs output"
    - "When DB_PATH is unset, app falls back to tennis.db in project root"
  artifacts:
    - path: "docker-requirements.txt"
      provides: "Pinned production-only Python dependencies"
      contains: "Flask==3.1.1"
    - path: ".dockerignore"
      provides: "Build context exclusions"
      contains: ".git"
    - path: "Dockerfile"
      provides: "Multi-stage build definition"
      contains: "python:3.12-slim"
    - path: "entrypoint.sh"
      provides: "DB init + exec gunicorn startup script"
      contains: "exec /venv/bin/gunicorn"
    - path: "app.py"
      provides: "DB_PATH env var support and /health endpoint"
      contains: "DB_PATH"
  key_links:
    - from: "Dockerfile"
      to: "docker-requirements.txt"
      via: "COPY and pip install"
      pattern: "COPY docker-requirements.txt"
    - from: "Dockerfile"
      to: "entrypoint.sh"
      via: "ENTRYPOINT directive"
      pattern: 'ENTRYPOINT.*entrypoint.sh'
    - from: "entrypoint.sh"
      to: "app.py"
      via: "python -c import and gunicorn app:app"
      pattern: "from app import app"
    - from: "app.py"
      to: "DB_PATH env var"
      via: "os.environ.get in config section"
      pattern: 'os.environ.get.*DB_PATH'
    - from: "Dockerfile"
      to: "HEALTHCHECK"
      via: "urllib.request to /health"
      pattern: "localhost:5000/health"
---

<objective>
Write the complete Docker infrastructure for the Flask-SocketIO tennis app: multi-stage Dockerfile, entrypoint script, dependency file, dockerignore, and the two app.py modifications (DB_PATH env var + /health endpoint).

Purpose: This is the entire deliverable of Phase 1 — a working Docker image that runs the production app correctly with single eventlet worker, non-root user, configurable database path, and health check.

Output: Dockerfile, docker-requirements.txt, .dockerignore, entrypoint.sh (new files); app.py, backup_tennis_db.sh (modified files)
</objective>

<execution_context>
@/home/nuc8/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuc8/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-container/01-CONTEXT.md
@.planning/phases/01-app-container/01-RESEARCH.md

<interfaces>
<!-- Key code locations the executor needs. Extracted from app.py. -->

From app.py line 91 (database config — MUST be modified):
```python
app.config["DATABASE"] = os.path.join(app.root_path, "tennis.db")
```

From app.py line 115 (CSRF instance — needed for @csrf.exempt on health route):
```python
csrf = CSRFProtect(app)
```

From app.py lines 393-444 (init_db — called by entrypoint, has idempotency guard):
```python
def init_db():
    db = get_db()
    try:
        cur = db.execute(
            "SELECT name FROM sqlite_master WHERE type='table' AND name='players';"
        )
        table_exists = cur.fetchone()
        if not table_exists:
            # ... runs schema.sql and seeds initial_players.json
```

From app.py lines 2609-2616 (main block — init_db only called here, NOT by gunicorn):
```python
if __name__ == "__main__":
    with app.app_context():
        init_db()
    logger.info("Starting Flask-SocketIO server with eventlet...")
    host = os.environ.get("FLASK_HOST", "0.0.0.0")
    port = int(os.environ.get("FLASK_PORT", 5000))
    socketio.run(app, host=host, port=port, debug=debug_mode, use_reloader=debug_mode)
```

From backup_tennis_db.sh line 15 (hardcoded path — MUST be updated):
```bash
SOURCE_DB="/home/stecher/stecher_tennis/tennis.db"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker infrastructure files</name>
  <files>docker-requirements.txt, .dockerignore, Dockerfile, entrypoint.sh</files>
  <read_first>
    - app.py (lines 36-120 for config/startup, lines 368-444 for DB functions, lines 2609-2616 for main block)
    - .planning/phases/01-app-container/01-RESEARCH.md (exact file contents for all four files)
    - prod-requirements.txt (reference, do NOT modify)
  </read_first>
  <action>
    Create four new files in the project root:

    **1. docker-requirements.txt** — Exact content (pinned from conda env):
    ```
    Flask==3.1.1
    Flask-SocketIO==5.5.1
    Flask-Login==0.6.3
    Flask-WTF==1.2.2
    flask-cors==6.0.1
    gunicorn==23.0.0
    eventlet==0.40.1
    greenlet==3.1.1
    python-engineio==4.11.2
    python-socketio==5.12.1
    bcrypt==4.3.0
    Werkzeug==3.1.3
    itsdangerous==2.2.0
    Jinja2==3.1.6
    click==8.2.1
    python-dotenv==1.1.0
    pydantic==2.11.4
    pydantic_core==2.33.2
    requests==2.32.3
    ```

    **2. .dockerignore** — Exclude per CONT-03 requirement. Must include:
    `.git`, `.gitignore`, `__pycache__/`, `*.py[cod]`, `*.pyo`, `*.pyd`, `.Python`, `*.egg-info/`, `dist/`, `build/`, `.eggs/`, `requirements.txt`, `environment.yml`, `*.db`, `*.sqlite`, `*.sqlite3`, `documentation/`, `*.html`, `*.md` (but NOT CLAUDE.md — use `!CLAUDE.md`), `.env`, `.env.*`, `.planning/`, `.claude/`, `backup/`, `test/`, `tests/`, `pytest.ini`, `setup.cfg`, `pyproject.toml`, `.vscode/`, `.idea/`, `*.swp`, `*.swo`, `.DS_Store`, `Thumbs.db`

    **3. Dockerfile** — Multi-stage build with two stages:

    Stage 1 "builder":
    - `FROM python:3.12-slim AS builder`
    - `WORKDIR /build`
    - Install build-essential and libffi-dev (needed for bcrypt, greenlet C extensions)
    - `COPY docker-requirements.txt .`
    - Create venv at `/venv`, upgrade pip with `--no-cache-dir`, install requirements with `--no-cache-dir`

    Stage 2 "runtime":
    - `FROM python:3.12-slim AS runtime`
    - Create group `appgroup` (GID 1000) and user `appuser` (UID 1000, no home dir, shell /sbin/nologin)
    - `WORKDIR /app`
    - `COPY --from=builder /venv /venv`
    - Copy app files with `--chown=appuser:appgroup`: `app.py`, `schema.sql`, `initial_players.json`, `templates/`, `static/`, `entrypoint.sh`
    - `RUN mkdir -p /app/data && chown appuser:appgroup /app/data`
    - `RUN chmod +x /app/entrypoint.sh`
    - `USER appuser`
    - `ENV PATH="/venv/bin:$PATH"`
    - `ENV DB_PATH=/app/data/tennis.db`
    - `EXPOSE 5000`
    - HEALTHCHECK: `--interval=30s --timeout=10s --start-period=15s --retries=3` using `python -c "import urllib.request; urllib.request.urlopen('http://localhost:5000/health')"` with `|| exit 1`
    - `ENTRYPOINT ["./entrypoint.sh"]`

    **4. entrypoint.sh** — Bash script with `set -euo pipefail`:
    - Print "Initializing database at ${DB_PATH:-/app/data/tennis.db}..."
    - Run `python -c "from app import app, init_db; ..."` with `app.app_context()` wrapping `init_db()`
    - `exec /venv/bin/gunicorn` with flags: `--workers 1 --worker-class eventlet --bind 0.0.0.0:5000 --timeout 120 --access-logfile - --error-logfile - --log-level info app:app`
    - The `exec` is critical — it replaces the shell process so gunicorn is PID 1 and receives Docker signals directly.
    - First line must be `#!/bin/bash`

    Do NOT use `--preload` flag with gunicorn (known incompatibility with eventlet).
  </action>
  <verify>
    <automated>test -f docker-requirements.txt && test -f .dockerignore && test -f Dockerfile && test -f entrypoint.sh && grep -q "Flask==3.1.1" docker-requirements.txt && grep -q "python:3.12-slim" Dockerfile && grep -q "exec /venv/bin/gunicorn" entrypoint.sh && grep -q ".git" .dockerignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docker-requirements.txt contains exactly 19 pinned packages starting with `Flask==3.1.1` and ending with `requests==2.32.3`
    - docker-requirements.txt does NOT contain google-generativeai, openai, numpy, scipy, Pillow, beautifulsoup4, litellm, posthog, or mixpanel
    - .dockerignore contains `.git`, `__pycache__/`, `documentation/`, `*.db`, `.env`, `.planning/`
    - .dockerignore contains `!CLAUDE.md` (negation to keep CLAUDE.md)
    - Dockerfile contains `FROM python:3.12-slim AS builder` and `FROM python:3.12-slim AS runtime`
    - Dockerfile contains `groupadd --gid 1000 appgroup` and `useradd --uid 1000 --gid 1000`
    - Dockerfile contains `USER appuser`
    - Dockerfile contains `ENV DB_PATH=/app/data/tennis.db`
    - Dockerfile contains `HEALTHCHECK` with `localhost:5000/health`
    - Dockerfile contains `ENTRYPOINT ["./entrypoint.sh"]`
    - Dockerfile contains `COPY --chown=appuser:appgroup app.py schema.sql initial_players.json`
    - Dockerfile contains `mkdir -p /app/data && chown appuser:appgroup /app/data`
    - entrypoint.sh starts with `#!/bin/bash` and contains `set -euo pipefail`
    - entrypoint.sh contains `from app import app, init_db`
    - entrypoint.sh contains `exec /venv/bin/gunicorn`
    - entrypoint.sh contains `--workers 1 --worker-class eventlet --bind 0.0.0.0:5000 --timeout 120`
    - entrypoint.sh contains `--access-logfile - --error-logfile -`
    - entrypoint.sh does NOT contain `--preload`
    - prod-requirements.txt is UNCHANGED (not modified or overwritten)
  </acceptance_criteria>
  <done>All four Docker infrastructure files exist with correct content. prod-requirements.txt untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add /health endpoint, DB_PATH env var to app.py, update backup script</name>
  <files>app.py, backup_tennis_db.sh</files>
  <read_first>
    - app.py (full file — lines 85-120 for config section where DB_PATH goes, line 91 specifically, line 115 for csrf instance, lines 368-444 for get_db/init_db)
    - backup_tennis_db.sh (line 15 for SOURCE_DB hardcoded path)
    - .planning/phases/01-app-container/01-RESEARCH.md (Pattern 3 and Pattern 4 sections for exact code)
    - .planning/phases/01-app-container/01-CONTEXT.md (locked decisions for health endpoint and DB_PATH)
  </read_first>
  <action>
    **1. DB_PATH env var in app.py** — Replace line 91 (`app.config["DATABASE"] = os.path.join(app.root_path, "tennis.db")`) with:
    ```python
    _db_path = os.environ.get("DB_PATH")
    if _db_path:
        # Docker mode: use configured path, auto-create parent dirs
        _db_dir = os.path.dirname(_db_path)
        if _db_dir:
            os.makedirs(_db_dir, exist_ok=True)
        app.config["DATABASE"] = _db_path
    else:
        # Non-Docker mode: keep current behavior (project root)
        app.config["DATABASE"] = os.path.join(app.root_path, "tennis.db")
    ```
    This preserves backward compatibility: when `DB_PATH` is not set, behavior is identical to current code.

    **2. /health endpoint in app.py** — Add immediately after the `csrf = CSRFProtect(app)` line (line 115), before the Flask-Login section:
    ```python
    @app.route("/health")
    @csrf.exempt
    def health():
        return jsonify({"status": "ok"}), 200
    ```
    This route has no `@login_required` so it is publicly accessible. The `@csrf.exempt` decorator ensures Docker HEALTHCHECK and curl work without CSRF tokens. `jsonify` is already imported at the top of app.py.

    **3. Update backup_tennis_db.sh** — Change line 15 from:
    ```bash
    SOURCE_DB="/home/stecher/stecher_tennis/tennis.db"
    ```
    to:
    ```bash
    SOURCE_DB="${DB_PATH:-/home/stecher/stecher_tennis/tennis.db}"
    ```
    This uses `DB_PATH` env var if set (Docker context), otherwise falls back to the current hardcoded path (RPi production context). The rest of the script remains unchanged.
  </action>
  <verify>
    <automated>grep -q 'os.environ.get("DB_PATH")' app.py && grep -q '@csrf.exempt' app.py && grep -q 'def health' app.py && grep -q 'jsonify({"status": "ok"})' app.py && grep -q 'DB_PATH:-' backup_tennis_db.sh && python -c "import app; print('import ok')" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - app.py contains `_db_path = os.environ.get("DB_PATH")`
    - app.py contains `os.makedirs(_db_dir, exist_ok=True)` (auto-create parent dirs)
    - app.py contains `app.config["DATABASE"] = _db_path` (Docker path)
    - app.py contains `app.config["DATABASE"] = os.path.join(app.root_path, "tennis.db")` (fallback path)
    - app.py contains `@app.route("/health")`
    - app.py contains `@csrf.exempt` immediately before or after the health route decorator
    - app.py contains `def health():` followed by `return jsonify({"status": "ok"}), 200`
    - app.py does NOT contain `@login_required` on the health route
    - backup_tennis_db.sh line 15 reads `SOURCE_DB="${DB_PATH:-/home/stecher/stecher_tennis/tennis.db}"`
    - `python -c "import app"` exits 0 (no import errors introduced)
    - The original `app.config["DATABASE"] = os.path.join(app.root_path, "tennis.db")` on line 91 is REPLACED (not duplicated)
  </acceptance_criteria>
  <done>app.py has /health endpoint (CSRF-exempt, no auth) and DB_PATH env var support with fallback. backup_tennis_db.sh respects DB_PATH. App imports without error.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -c "import app"` — app.py imports without error
2. `grep -c "DB_PATH" app.py` — returns 2+ (env var read + config assignment)
3. `grep "def health" app.py` — health endpoint exists
4. `grep "exec /venv/bin/gunicorn" entrypoint.sh` — entrypoint execs gunicorn
5. `wc -l docker-requirements.txt` — 19 lines (19 packages)
6. `head -1 Dockerfile` — starts with `FROM python:3.12-slim`
</verification>

<success_criteria>
- All 6 files exist (4 new, 2 modified) with correct content
- app.py imports without error after modifications
- No existing functionality broken (DB_PATH fallback preserves original behavior)
- Docker infrastructure is complete and ready for build testing in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-container/01-01-SUMMARY.md`
</output>
