---
phase: 03-https-via-caddy
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: false
requirements: [HTTP-01, HTTP-02, HTTP-03, HTTP-04]

user_setup:
  - service: duckdns
    why: "DNS-01 ACME challenge requires valid DuckDNS credentials"
    env_vars:
      - name: DUCKDNS_TOKEN
        source: "DuckDNS account page (https://www.duckdns.org) — copy token for the 'nechvatal' domain"
      - name: ACME_EMAIL
        source: "Your email address for Let's Encrypt registration"
    dashboard_config:
      - task: "Ensure 'nechvatal' domain is registered on your DuckDNS account"
        location: "https://www.duckdns.org"

must_haves:
  truths:
    - "Custom Caddy image builds and includes dns.providers.duckdns module"
    - "Caddy starts and obtains a staging TLS certificate from Let's Encrypt"
    - "HTTPS request to port 10443 reaches the Flask app"
    - "HTTP request to port 80 redirects to HTTPS on port 10443"
    - "WebSocket (Socket.IO) connects over wss:// through Caddy"
    - "ACME_CA env var controls which ACME endpoint Caddy uses"
  artifacts: []
  key_links:
    - from: "Dockerfile.caddy"
      to: "caddy binary"
      via: "xcaddy build with duckdns module"
      pattern: "dns.providers.duckdns"
    - from: "Caddyfile"
      to: "Let's Encrypt staging"
      via: "ACME_CA env var"
      pattern: "acme-staging"
    - from: "Caddy HTTPS"
      to: "Flask app"
      via: "reverse_proxy app:5000"
      pattern: "reverse_proxy"
---

<objective>
Validate the complete HTTPS stack by building the custom Caddy image, starting the Compose stack with DuckDNS credentials, and verifying all Phase 3 requirements with staging certificates.

Purpose: Prove that all four HTTP requirements (HTTP-01 through HTTP-04) work end-to-end before declaring Phase 3 complete.
Output: Validated HTTPS stack with staging certificate, all smoke tests passing.
</objective>

<execution_context>
@/home/nuc8/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuc8/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-https-via-caddy/03-CONTEXT.md
@.planning/phases/03-https-via-caddy/03-RESEARCH.md
@.planning/phases/03-https-via-caddy/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01 creates these files. Executor reads them to verify correctness. -->

From Dockerfile.caddy (created by Plan 01):
- Multi-stage: caddy:2-builder-alpine -> caddy:2-alpine
- xcaddy build --with github.com/caddy-dns/duckdns

From Caddyfile (rewritten by Plan 01):
- Global: acme_ca {$ACME_CA}, email {$ACME_EMAIL}
- HTTPS: {$DUCKDNS_DOMAIN}.duckdns.org:{$HTTPS_PORT} with tls { dns duckdns {$DUCKDNS_TOKEN} }
- HTTP redirect: :80 { redir ... permanent }
- Reverse proxy: app:5000 with header_up directives

From docker-compose.yml (updated by Plan 01):
- caddy service: build { dockerfile: Dockerfile.caddy }, ports 10443+80, env_file: .env

From .env.example (updated by Plan 01):
- DUCKDNS_DOMAIN, DUCKDNS_TOKEN, ACME_EMAIL, ACME_CA, HTTPS_PORT
- ACME_CA default: staging URL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build custom Caddy image and verify duckdns module</name>
  <read_first>
    - Dockerfile.caddy
    - docker-compose.yml
  </read_first>
  <files></files>
  <action>
Build the custom Caddy image and verify it includes the duckdns DNS provider module.

**Step 1: Build the image**
```bash
docker compose build caddy
```
This must complete without error. The xcaddy build step compiles Caddy with the caddy-dns/duckdns module.

**Step 2: Verify the module is present**
```bash
docker compose run --rm caddy caddy list-modules | grep duckdns
```
Expected output: `dns.providers.duckdns`

If the module is NOT listed, the Dockerfile.caddy is incorrect (the `--with github.com/caddy-dns/duckdns` line is missing or misspelled).

**Step 3: Verify existing tests still pass**
```bash
pytest tests/ -x -q
```
All 7 existing tests must pass (no app.py changes were made, but confirm no regressions).

This task validates HTTP-01 (custom Caddy image built with xcaddy + caddy-dns/duckdns module).
  </action>
  <verify>
    <automated>cd /home/nuc8/05_development/02_python/01_stecher_tennis && docker compose build caddy 2>&1 | tail -5 && docker compose run --rm caddy caddy list-modules 2>/dev/null | grep duckdns</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose build caddy` exits with code 0
    - `docker compose run --rm caddy caddy list-modules | grep duckdns` outputs `dns.providers.duckdns`
    - `pytest tests/ -x -q` exits 0 with all 7 tests passing
  </acceptance_criteria>
  <done>Custom Caddy image builds successfully and includes dns.providers.duckdns module. HTTP-01 validated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test HTTPS stack with staging certificate</name>
  <read_first>
    - .env.example
    - Caddyfile
    - docker-compose.yml
    - .planning/phases/03-https-via-caddy/03-CONTEXT.md
  </read_first>
  <files></files>
  <action>
Run the full HTTPS smoke test suite. Before starting the stack, the user must have a `.env` file with real DuckDNS credentials.

**Pre-flight checks (automation runs these):**
1. Verify `.env` exists and has real DUCKDNS_TOKEN (not `your-duckdns-token-here`)
2. Test DuckDNS API: `source .env && curl -s "https://www.duckdns.org/update?domains=${DUCKDNS_DOMAIN}&token=${DUCKDNS_TOKEN}&txt=test&verbose=true"` -- should return OK
3. Verify no port conflicts: `ss -tlnp | grep -E ':80|:10443'`

**Start stack:**
4. `docker compose up -d`
5. Wait ~30 seconds for ACME challenge to complete
6. `docker compose logs caddy` -- check for certificate issuance (no errors)

**Smoke tests (automation runs, presents results to user):**

HTTP-01 (custom build):
- `docker compose exec caddy caddy list-modules | grep duckdns` -- output: `dns.providers.duckdns`

HTTP-02 (HTTPS with staging cert):
- `curl -vk https://localhost:10443/health 2>&1 | grep -E "issuer|HTTP/"` -- should show "Fake LE Intermediate" issuer and `HTTP/2 200`

HTTP-03 (WebSocket passthrough):
- User opens browser to `https://nechvatal.duckdns.org:10443` (accept staging cert warning), logs in, and confirms Socket.IO connects (real-time updates appear)

HTTP-04 (ACME toggle):
- `docker compose exec caddy sh -c 'echo $ACME_CA'` -- should show staging URL

HTTP redirect:
- `curl -sI http://localhost/health` -- Location header must contain `https://nechvatal.duckdns.org:10443/health` with HTTP 301

**Cleanup:**
- `docker compose down` after user approves
  </action>
  <verify>
    <automated>cd /home/nuc8/05_development/02_python/01_stecher_tennis && docker compose exec caddy caddy list-modules 2>/dev/null | grep duckdns && curl -sk https://localhost:10443/health -o /dev/null -w '%{http_code}' 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose up -d` starts both services without error
    - `docker compose exec caddy caddy list-modules | grep duckdns` outputs `dns.providers.duckdns`
    - `curl -vk https://localhost:10443/health 2>&1` shows issuer containing "Fake LE" and HTTP 200
    - `curl -sI http://localhost/health` returns 301 with Location containing `https://nechvatal.duckdns.org:10443`
    - Caddy logs show no ACME challenge errors
    - WebSocket connects over wss:// (browser test or log evidence)
  </acceptance_criteria>
  <done>All four HTTP requirements validated with staging certificate. Phase 3 HTTPS stack confirmed working end-to-end.</done>
</task>

</tasks>

<verification>
Phase 3 complete when all of these are true:
1. Custom Caddy image builds with duckdns module (HTTP-01)
2. Staging TLS certificate obtained via DNS-01 challenge (HTTP-02)
3. WebSocket connects through Caddy HTTPS (HTTP-03)
4. ACME_CA env var controls staging/production endpoint (HTTP-04)
5. HTTP-to-HTTPS redirect works with non-standard port 10443
6. All existing pytest tests pass
</verification>

<success_criteria>
The HTTPS stack works end-to-end with a staging certificate: browser navigates to https://nechvatal.duckdns.org:10443 and sees the app (with staging cert warning), HTTP redirects to HTTPS, and WebSocket connects over wss://. The user has approved the smoke test checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/03-https-via-caddy/03-02-SUMMARY.md`
</output>
