The Tennis Ranking List web application is used to manage tennis players and organize challenges between them.
In addition to displaying the current ranking list, the application offers functions for dynamically updating
the ranking positions and for managing player blocking and availability.

Backend: Python with the Flask framework
Real-time communication: Flask-SocketIO
Frontend: HTML, CSS, JavaScript (jQuery, Bootstrap)
Database: SQLite
JSON data for initializing the players (initial_players.json)

I have found an logic error within admin.html and its important to fix it:

Pending challenges admin.html:

The administrator selects the winner from the drop-down list and must enter the correct final result.
Only correct entries may be allowed and saved in the database. The final result must be displayed in index.html
under Challenges completed.

best of three means that a match consists of a maximum of three sets, and the player who wins two sets first
wins the match. A player must win two out of three sets to win the match. If after two sets each player has won one set,
the third set decides the winner.

If the opponent wins, the following examples are correct:

Examples of correct final results Opponent wins:
Example 1:
Set 1: 3:6
Set 2: 3:6

Example 2:
Set 1: 1:6
Set 2: 6:7(6:8)

Example 3:
Set 1: 4:6
Set 2: 6:3
Set 3: 0:6
